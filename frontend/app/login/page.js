import LoginForm from "./components/loginForm";

export default function LoginPage() {
    return (
        <main className="auth-page">
            <h1>Login</h1>

            <LoginForm />
        </main>
    );
}